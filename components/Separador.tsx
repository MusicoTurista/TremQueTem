import { View } from 'react-native';

export default function Separador() {
    return (
        <View
            style={{
                height: 5,
                backgroundColor: '#f78f25',
                marginVertical: 15,
                borderRadius: 200,
            }}
        />
    )
}